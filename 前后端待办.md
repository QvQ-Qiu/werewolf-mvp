# 0523 狼人杀 · 前后端对接状态

> 更新：2026-05-23 — **第三节待办已全部实现**（开发者 Agent 面板仍属可选扩展）。

## 已实现清单

### 对局主流程
- REST：`POST/GET /games`、`GET /games` 列表、`GET /games/{id}/replay`（token 鉴权）、`GET /games/{id}/traces/{seat}`
- WebSocket：发言/投票/夜晚、心跳 `PING`、局时 `GAME_TIMEOUT`、`game_deadline_ts`
- 复盘：局终落盘 `app/data/replays/{id}.json`，重启后可读；前端 `sessionStorage` 离线兜底
- `LlmTrace`：`phase_ref`、`messages_full`（完整 prompt 在 traces 接口与落盘 state 中）
- `GameReplayResponse`：`belief_by_seat`、`player_memories`
- 前端：`filtered_view` 合并、大厅继续对局、`ws_url`、复盘信念链展示

### 部署
- FastAPI 托管 `frontend/dist`（`SERVE_FRONTEND_DIST`）
- `0523/docker-compose.yml` + `backend/Dockerfile`

### 未做（刻意保留）
- Web 开发者面板（Skills/MCP/OpenClaw/手动 advance）— 仅 CLI/MCP 使用

## 验收

```bash
# 后端测试
cd 0523/backend && .venv/bin/pytest tests -v

# 前端类型检查
cd 0523/frontend && npx tsc -b

# 本地运行
cd 0523/backend && uvicorn app.main:app --reload --port 8000
cd 0523/frontend && npm run dev
```
